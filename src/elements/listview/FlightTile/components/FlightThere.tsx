import React from 'react'
import s from '../FlightTile.module.scss'
import { Flight } from './Flight'

interface Props { }

export const FlightThere = (props: Props) => {
  return (
    <Flight />
  )
}