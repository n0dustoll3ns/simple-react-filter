import React from 'react'
import s from '../FlightTile.module.scss'
import { Flight } from './Flight'

type Props = {}

export const FlightBack = (props: Props) => {
  return (
      <Flight />
    )
}